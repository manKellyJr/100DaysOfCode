# T-Shirt function
def t_shirt(size, message):
     """Function that prints that T-shirt size and the message to be writen on the T-Shirt"""
     print("Size: " + size.upper() + "\nMessage" + message.title())

t_shirt('l', "H.T.M.L")
